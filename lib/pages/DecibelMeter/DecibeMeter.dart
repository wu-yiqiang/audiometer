import 'package:flutter/material.dart';
import 'package:get/get.dart';

class DecibeMeterPage extends StatelessWidget {
  Widget build(BuildContext context) {
    return Scaffold(body: SafeArea(child: Text("分贝仪")));
  }
}
